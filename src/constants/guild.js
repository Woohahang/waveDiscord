const GUILD_CHANNEL_TYPES = {
    MAIN: 'mainChannelId',
    ADMIN: 'adminChannelId',
};

const VALID_CHANNEL_TYPES = Object.values(GUILD_CHANNEL_TYPES);

module.exports = {
    GUILD_CHANNEL_TYPES,
    VALID_CHANNEL_TYPES
};