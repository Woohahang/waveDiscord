// nicknameModalCreator.js

const { gameLabels, description } = require('../../module/games/gameData');

module.exports = async (interaction) => {
    // try {

    //     const gameTitle = interaction.values[0];

    //     console.log('gameTitle : ', gameTitle);

    //     const m = {
    //         title: description[gameTitle], customId: gameTitle
    //     }

    //     console.log(m);

    // } catch (error) {
    //     console.error('nicknameModalCreator.js 에러 : ', error);
    // };
};